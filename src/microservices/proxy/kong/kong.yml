_format_version: "3.0"
_transform: true

services:
  - name: monolith
    url: ${MONOLITH_URL}
    routes:
      - name: monolith-route
        paths:
          - /
  - name: movies-service
    enabled: ${GRADUAL_MIGRATION}
    url: ${MOVIES_SERVICE_URL}
    routes:
      - name: movies-route
        paths:
          - /api/movies/
  - name: events-service
    url: ${EVENTS_SERVICE_URL}
    routes:
      - name: events-route
        paths:
          - /api/events/

upstreams:
  - name: movies-service
    targets:
      - target: ${MOVIES_SERVICE_HOST}
        weight: ${MOVIES_MIGRATION_PERCENT}
        failover: true